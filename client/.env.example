# Client environment
# Default uses the Vite dev proxy (/api → http://localhost:5050).
# Port 5050 avoids clashing with macOS AirPlay/Control Center on port 5000.
# For Netlify, set VITE_API_URL=https://dr-shivalika-saraswat-dental-1.onrender.com/api
VITE_API_URL=/api
